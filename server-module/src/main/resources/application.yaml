spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Zap #?currentSchema=zapschema
    username: postgres
    password: admin
    driverClassName: org.postgresql.Driver
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format-sql: true

laximo:
  login: 123
  secretKey: 123